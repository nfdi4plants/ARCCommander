namespace ArcCommander

open System

module Arc =

    // Creates Arc specific folder structure 
    let init (name:string) =
        NotImplementedException() |> raise

    // Returns true if called anywhere in an arc 
    let isArc () =
        NotImplementedException() |> raise

    let addAssay (name:string) = 
        NotImplementedException() |> raise